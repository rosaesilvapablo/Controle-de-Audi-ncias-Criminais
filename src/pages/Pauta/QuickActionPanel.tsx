import type { Audiencia } from '../../types'

interface QuickActionPanelProps {
  open: boolean
  audiencia: Audiencia | null
  onClose: () => void
}

export function QuickActionPanel(_props: QuickActionPanelProps) {
  return null
}
